{
  "name": "ticket-backend-root",
  "version": "1.0.0",
  "private": true,
  "description": "Shared backend root to install shared dependencies for microservices",
  "scripts": {
    "postinstall": "echo 'backend dependencies installed'"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  }
}